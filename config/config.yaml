server:
  # 微信云托管必须监听 80 端口
  port: 80
  mode: debug

database:
  # 这里的 DSN 只是本地兜底用，线上会优先读取云托管的环境变量
  dsn: "root:123456@tcp(127.0.0.1:3306)/yuedi_edu?charset=utf8mb4&parseTime=True&loc=Local"
  max_idle_conns: 10
  max_open_conns: 100

jwt:
  secret: "yuedi_edu_super_secret_key_2026"
  expire_hours: 72

wechat:
  app_id: "wx4ebf6ad38cff5270"
  app_secret: "请替换为真实的AppSecret"
  mch_id: "请替换为微信商户号"
  mch_api_v3_key: "请替换为APIv3密钥"

agora:
  app_id: "请替换为声网AppID"
  app_certificate: "请替换为声网App证书"
